#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>

int main() {
	setlocale(LC_ALL, "Russian");
	int x, i=0, c;
	scanf("%d", &x);
	for(; pow(2, i) < x; i++) {c = (x>>i)&1; printf("%d", c);};

	return 0;
}
